# config/env.yaml
# 这个文件用来存放不同环境的地址和账号密码
# 比如 dev、test、prod 三个环境，可以在这里统一管理

environments:
  # 测试环境
  test:
    base_url: "https://test-platform.easycontrol.io/mdm-web/Login"
    # 账号密码已移除，改为通过环境变量注入（Jenkins/本地 .env）
    username: "${MDM_USERNAME}"
    password: "${MDM_PASSWORD}"
